#include <bits/stdc++.h>
#include <iostream>
#include <cmath>
#include <vector>
using namespace std;
#define rep(i, s, n) for (int i = (s); i < (n); i++)


int main()
{
    int x;
    cin >> x;

    int n, m;
    cin >> n >> m;
    int a[n];
    rep(i, 0, n) cin >> a[i];

    cout << x << endl;
}