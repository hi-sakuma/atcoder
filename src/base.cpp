#include <iostream>
#include <cmath>
#include <vector>
using namespace std;
#define rep(i, s, n) for (int i = (s); i < (n); i++)

int main()
{
    int x;
    cin >> x;
    cout << x << endl;
}